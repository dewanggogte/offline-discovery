# =============================================================================
# AC Price Caller — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your actual keys.
# NEVER commit .env.local to git.

# --- Sarvam AI (https://dashboard.sarvam.ai) ---
# Sign up for free ₹1,000 credits
SARVAM_API_KEY=your_sarvam_api_key_here

# --- OpenAI (https://platform.openai.com) ---
# Used for the conversational LLM (GPT-4o-mini)
# Alternative: Use Sarvam's own LLM for lower cost, but less reliable extraction
OPENAI_API_KEY=sk-your_openai_key_here

# --- LiveKit Cloud (https://cloud.livekit.io) ---
# Free tier includes 5,000 participant-minutes/month
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# --- Telnyx (https://telnyx.com) ---
# SIP trunk provider for making actual phone calls.
# Setup steps:
#   1. Sign up at telnyx.com, buy a US number
#   2. Create an Outbound Voice Profile (API: POST /v2/outbound_voice_profiles)
#   3. Create an FQDN Connection with username/password (API: POST /v2/fqdn_connections)
#   4. Register your LiveKit SIP endpoint as an FQDN (API: POST /v2/fqdns)
#   5. Associate your phone number to the connection (API: PATCH /v2/phone_numbers/<id>)
#   6. Create an outbound trunk in LiveKit Cloud dashboard (Telephony → SIP Trunks)
#      using address "sip.telnyx.com" and the credentials from step 3
#   7. Copy the LiveKit trunk ID (starts with ST_) below
TELNYX_API_KEY=your_telnyx_api_v2_key_here
TELNYX_PHONE_NUMBER=+1XXXXXXXXXX
SIP_OUTBOUND_TRUNK_ID=your_livekit_trunk_id_here
